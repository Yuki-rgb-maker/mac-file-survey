# 최종 정리 — 무엇을 만들고, 어떻게, 어떤 순서로

작성 시점: P0 실측 직전. 코드는 `p0_survey.py` 하나뿐입니다.
이 문서는 논의에서 **확정된 것만** 담습니다. 아직 안 정한 것은 마지막 장에 따로 모았습니다.

---

## 0. 한 줄

> **포토샵은 배웠는데 폴더는 아무도 안 가르쳐줬다.**

작업하다 쌓인 것들을 정리하는 맥 도구 시리즈.
정리 규칙을 대신 만들어 주고, 언제든 되돌릴 수 있게 합니다.

### 타깃

| | |
|---|---|
| 핵심 | 2~30대 미대·디자인대생, 초보 디자이너·크리에이터 |
| 첫 사용자 | **본인.** 1편이 잘 된 이유가 이것입니다 |
| 자연스러운 확장 | 제품디자인·건축(Rhino·Fusion), 영상, AI 크리에이터 |
| 지금은 제외 | 엔지니어링 CAD(SolidWorks·실무 AutoCAD), 시니어, 일반 "컴맹" |

**"컴맹"이라는 말을 쓰지 않습니다.** 미대생은 무능한 게 아니라 안 배운 것입니다.
도구는 가르치는데 파일 관리는 커리큘럼에 없습니다.

### 검증 규칙

> **실제로 아는 사람 다섯 명의 이름을 적을 수 있어야 타깃입니다.**
> 그리고 그 다섯 대의 맥에서 P0를 돌릴 수 있어야 합니다.

이름을 못 채우면 타깃이 아니라 상상입니다. 1편의 오탐 7건은 전부 실물 대조로 잡혔고,
그게 가능했던 이유는 본인이 사용자였기 때문입니다. 타깃을 넓히면 그 루프가 끊어집니다.

---

## 1. 시리즈 지도

```
1편  앱을 지운 뒤 남은 것        완료 · 5.5 GB 확보 검증
2편  쌓인 것을 구조로            기획 완료 · 착수 전
3편  만들다 남은 것              기획 완료 · 착수 전
4편  가족 모드                   보류 (조건 미충족)
```

**세 편이 같은 사람의 디스크입니다.**

| | 문제 | 왜 미대생에게 생기나 |
|---|---|---|
| 1편 | 앱 잔여물 | 툴을 이것저것 깔았다 지웁니다 (Figma·Blender·Procreate 체험판…) |
| 2편 | 5년치 무질서 | 작업물은 쌓이는데 규칙을 배운 적이 없습니다 |
| 3편 | `진짜최종.psd` × 15 | 디자이너 고유 문제. 회계사에게는 안 생깁니다 |

### 각 편이 파는 것

```
1편  안심     — 안 지워도 되는 걸 안 지웠구나
2편  자신감   — 내 파일을 내가 안다
3편  용량 + 판단 — 못 지우던 걸 지울 수 있게
```

---

## 2. 채택 / 기각 / 보류

### ✅ 채택

| | 내용 |
|---|---|
| **"규칙을 대신 만들어 드립니다"** | 핵심 문구. Sortio가 백로그 15%에 둔 자리 |
| **"진짜 준비된 자산"** | 상품의 정체. 건드리기 무서운 것 → 자신 있게 쓰는 것 |
| **기록으로 남기기** | 3편 핵심 발명. 파일은 지우고 썸네일 요약만 남김 |
| **검색 먼저** | 정리보다 검색을 먼저 만듦 (읽기 전용, 단독으로도 제품) |
| **증거 사다리** | 계보 > 지각해시/기하지문 > 파일명 |
| **개인화 학습** | 되돌리기 = 오답 레이블. 데이터를 안 내보냄 |
| **자동 백업 파일 정리** | 3D 첫 기능. `.blend1` 등 |

### ❌ 기각

| | 왜 |
|---|---|
| **2편의 용량·성능 약속** | 이동은 0바이트 확보. 사용자가 30초 안에 알아챕니다. `DECISIONS 7항` 위반 |
| **"모든 활동을 분석"** | 파일 앱에서 이 말은 감시로 읽힙니다 → "지금까지 쓰신 흔적을 읽어서" |
| **원격 제어** | **제품 모양이 테크서포트 스캠과 동일합니다.** ① 사칭당함 ② "이런 창은 믿어도 된다"를 학습시킴. 자식이 원하는 건 조작이 아니라 확인이라, **원격 요약만으로 가치의 80%가 나옵니다** |
| **모델을 앱에 번들** | 공간 없어서 온 사람에게 4GB를 요구하는 앞뒤 안 맞는 구조. 첫 실행 시 선택적 다운로드로 |
| **모델 재학습(파인튜닝)** | 병목이 지능이 아니라 증거라 효과가 작고, 데이터 수집 비용이 큽니다 |
| **오사우르스 필수 의존** | Apple Silicon 요구가 타깃과 충돌 |
| **레이어 개수 = 진척도** | 반대입니다. 최종본일수록 병합해서 레이어가 줄어듭니다 |

| **SolidWorks·DWG 지원** | 포맷 비공개, 타깃 밖 |

**단, 용량을 버리는 게 아니라 옮깁니다.**

```
❌ "정리하면 용량이 늘어납니다"
✅ "정리하고 나면, 지워도 되는 게 뭔지 비로소 보입니다"

2편(정리) → "이제 뭘 지울지 보이시죠?" → 1편(삭제) / 3편(버전) → 용량
```

정리는 삭제의 **전제조건**입니다. 5년치가 뒤엉켜 있으면 판단이 안 서서 아무것도 못 지웁니다.
2편이 용량을 약속하면 거짓말이지만, **2편이 1·3편으로 넘겨주면** 사실입니다.

### ⏸ 보류

| | 조건 |
|---|---|
| **자동 관리 (상시 감시 + 주간 추천)** | 신뢰가 쌓인 뒤. P12 |
| **4편 가족 모드** | 검색 엔진 완성 후 재검토. 조건 미충족: ① 시니어 반복 접근 경로 없음(부모님이 윈도우) ② 코드 서명·공증 필요 ③ 검색 엔진 미완 ④ 3편으로 타깃이 정반대 방향(크리에이터)으로 감 |
| **삭제 기능 (2편)** | 2편은 이동만. 삭제는 1편·3편의 몫 |
| **첫 스캔 비용 설계** | P0 실측 후 확정. 아래 참조 |
| **소유 구분 (남의 작업물)** | ⚠ 측정도 아직 못 믿습니다 — 느려서 표본을 줄인 것이라 **얼마나 놓치는지 안 쟀습니다.** 필요해지면 `--profile` 로 어디가 느린지 먼저 재고 제대로 고쳐야 합니다. 그리고 **표본 편향 주의.** 초기 협조자 9명이 전원 같은 유학원 동료라, 전원에게서 신호가 나와도 시장 특성이 아니라 표본 특성일 수 있습니다. **직업 경로가 다른 사용자 3명 이상**에게서 나와야 기능으로 확정 |

---

## 3. 확정된 원칙

### 3-1. 안전 (1편에서 계승)

```
관문 1  보호 경로 · 상대경로 · 심볼릭링크      동의로도 못 넘음
관문 2  재생성 가능한 종류인가                동의로 넘음
관문 3  허용된 뿌리 안인가                    동의로도 못 넘음
```

**2·3편에서 관문 1에 추가되는 것:**

| 대상 | 사고 |
|---|---|
| 클라우드 동기화 | iCloud·Dropbox·Drive·OneDrive·Creative Cloud. 모든 기기에 전파, 플레이스홀더 다운로드 폭발 |
| 앱 소유 라이브러리 | `.photoslibrary`·`.fcpbundle`·`.lrcat`. **폴더처럼 보이지만 파일 하나** |
| **Fusion 360 로컬 캐시** | 진짜 파일은 클라우드에 있음 |
| 개발 디렉터리 | `.git`·`node_modules`·`venv` |
| **참조가 걸린 파일** | PSD 링크 이미지, 어셈블리 파트, 프로젝트 소스 |

### 3-2. 되돌리기

```
1편·3편   삭제  →  vault 격리 → 30일 → 영구 삭제      (기존 기계 재사용)
2편       이동  →  journal (원래 경로 → 새 경로)      백업 불필요
```

**2편에는 백업이 필요 없습니다.** 파일이 사라지지 않고 자리만 바뀌니까요.
장부 한 줄이면 100% 되돌릴 수 있고 디스크를 1바이트도 안 씁니다. 기한도 필요 없습니다.

단, 되돌릴 때 **실물 대조**는 필수입니다 (`DECISIONS 9항`). 그 사이 사용자가
파일을 또 옮겼거나 지웠을 수 있습니다.

**대량 이동의 트랜잭션:**
- 이동 전에 장부를 먼저 쓰고, 이동 후에 확인 표시 (write-ahead)
- 중단되면 **이어서 하기 또는 되돌리기**를 고를 수 있어야 함
- `except: pass` 금지. 3만 개 중 200개가 조용히 실패하면 사용자는 영원히 모릅니다

### 3-3. LLM

> **이름이 붙은 데이터는 컴퓨터 밖으로 나가지 않습니다.**

| 작업 | 처리량 | 방식 |
|---|---|---|
| 파일 분류 | 수만 건 | **규칙** |
| 검색 인덱싱 | 수십만 건 | **규칙** |
| 쿼리 해석 | 1건 | LLM 있으면 좋음 |
| 페르소나 분석 | 1건 | LLM 있으면 좋음 |
| 설명 문장·채팅 | 소량 | LLM 있으면 좋음 |

**대량 처리를 LLM으로 하지 않는 것은 검증된 판단입니다.**
Sortio(2년 운영, 200만 파일)도 AI 정렬을 실행당 5,000개로 제한하고,
큰 폴더는 결정론적 규칙을 쓰라고 안내합니다.

**모델 우선순위**

```
1순위  Apple Foundation Models   macOS 26+, 다운로드 0MB
2순위  Qwen 3.5 또는 Gemma 4     둘 다 Apache 2.0. 한국어면 Qwen
       (오사우르스가 켜져 있으면 자동 감지해서 경유)
기본   모델 없어도 100% 동작      문장이 템플릿일 뿐
```

**LLM은 판단자로 쓰되 실행자로 쓰지 않습니다.**
LLM은 계획서(JSON)만 반환하고, 실행은 `mover.py`가 관문을 통과시켜 합니다.
그래야 프롬프트 인젝션이 구조적으로 막히고, `selftest.py`가 검사할 대상이 남습니다.

### 3-4. 학습

| | 방식 | 데이터 밖으로? | 우선순위 |
|---|---|---|---|
| **개인화** | 그 맥 안에서만 | ❌ 안 나감 | **1순위** |
| 규칙 개선 | 오탐 신고 버튼 (보여주고 받기) | 통계만 | 2순위 |
| 모델 재학습 | — | 나감 | 안 함 |

**무료로 얻어지는 정답 레이블 네 가지:**

| 사용자 행동 | 신호 | 강도 |
|---|---|---|
| **되돌리기를 눌렀다** | 명백한 오답 | ★★★ |
| 애매한 폴더에서 손으로 옮겼다 | 정답 레이블 | ★★★ |
| 채팅으로 계획을 수정했다 | 선호 신호 | ★★ |
| 검색했는데 못 찾았다 | 분류 실패 | ★★ |

되돌리기는 사용자를 안심시키려고 만든 기능인데, 부수적으로 가장 정확한 오답 신호를 줍니다.

### 3-5. UX (1편에서 계승)

1. 질문을 최소로
2. 파일이 아니라 앱 이름과 결과로
3. **답할 수 있는 질문만** — "지울까요?" ❌ → "기억나세요?" ✅
4. 판단 근거를 우리가 만들어 줌
5. 몰라도 안전하게 — "모르겠어요"가 정식 선택지

**3편에 추가되는 것:**
- 한 번에 한 뭉치만 (15개를 동시에 보여주면 멈춥니다)
- 중단해도 손해 없게
- 확보한 용량을 즉시 보여주기 (진행 자체가 보상)
- **폴더 이름을 AI가 짓지 말고 사용자가 짓게 하기** (자기가 지은 이름은 찾습니다)

### 3-5b. 남의 맥에서 데이터를 받을 때

**동료 파일명 대부분은 비밀이 아닙니다.** `포스터_최종.psd`가 새어도 아무 일 없습니다.

문제는 딱 하나입니다.

```
동료가 동의할 수 있는 것:  자기 파일명
동료가 동의할 수 없는 것:  학생 이름 · NDA 걸린 클라이언트명
```

**제3자 정보이지 동료 정보가 아닙니다.** 동료가 대신 동의해 줄 수 없습니다.

#### 해법 — 가리지 말고 안 찍기

가리려고 하면 목록·규칙·검증이 따라붙고, 목록에 없는 것은 샙니다.
(성씨 목록 100개로 가려 봤지만 영문 이름을 못 잡았고, 코드가 150줄 늘었습니다)

**남의 맥 보고서에서 파일명은 어차피 쓸모가 없습니다.**

| 보고서 내용 | 내 맥 | 남의 맥 |
|---|---|---|
| 회수량·계보 %·튀는 폴더 | 필요 | **필요** |
| `클라이언트A_최종.psd` | 필요 (묶임이 맞는지 판단) | **쓸모없음** (그 프로젝트를 모름) |

→ **`--share` 모드는 파일명이 나오는 절을 통째로 뺍니다.** 10줄이면 되고,
안 찍으니 샐 게 없어 검증할 것도 없습니다.

---

### 3-5c. 첫 스캔은 오래 걸린다 — 설계 전제 (P0 후 확정)

P0 에서 파일 800개의 속을 읽는 데 10분이 걸렸습니다.
제품은 표본이 아니라 **전 파일**에서 읽어야 하므로 더 걸립니다.

| | P0 (조사 도구) | 제품 (2·3편) |
|---|---|---|
| 목적 | 무엇을 먼저 만들지 결정 | 파일을 실제로 분류 |
| 대상 | **표본 250개** | **전부** |
| 놓치면 | 판단이 조금 흔들림 | **그 파일이 미아가 됨** |

#### 결론 1 — 한 번 읽고 저장한다

`P1 인벤토리 DB` 에 파일 목록만이 아니라 **파낸 것까지** 저장합니다.

```
파일 목록 · 크기 · 시각
XMP 계보 (original_id · 저장 횟수)
썸네일 지문 (해시)
PSD 색공간 등 포맷별 값
마지막으로 읽은 시각          ← 파일이 안 바뀌었으면 다시 안 읽음

첫 스캔:  몇 분 ~ 몇십 분
그 뒤:    몇 초 (바뀐 것만)
```

#### 결론 2 — 첫 스캔 화면의 요구사항

> 오래 걸리는 건 괜찮습니다. **얼어붙은 것처럼 보이는 게 문제입니다.**

| 반드시 | |
|---|---|
| **예상 시간을 미리 고지** | 시작 전에 "약 N분 걸립니다" |
| **진행 상황이 계속 움직임** | 멈춘 게 아님을 보여줌 |
| **찾은 것을 보여줌** | 기다리는 동안 기대가 쌓임 |

기획서 2번(로딩) 화면이 생각보다 중요한 화면입니다.

#### 예상 시간은 P0 결과로 계산합니다

`XMP 검사 250개에 걸린 시간` × `작업 원본 총 개수` → 첫 스캔 예상 시간.

---

### 3-5d. 언어를 엔진에서 분리한다

**한국어에 완벽하게 맞추되, 나중에 다른 언어로 넓힐 수 있게** 둡니다.

```
언어를 모름 (엔진)    계보 · 해시 · 지문 · 시간 · 크기 · 참조 그래프 · 안전 관문
언어에 묶임 (사전)    버전 낱말 · 문자 대역 · 업무 단서 · 문장 만들기
```

`p0_survey.py` 에서 이미 분리했습니다 (`lang_ko.py`).
가짜 일본어 사전을 끼워 **엔진 코드를 한 줄도 안 고치고** 작동하는 것을
확인했습니다.

#### 다른 언어를 넣을 때 반드시 확인할 것

| | |
|---|---|
| **글자 조합 방식 (NFC/NFD)** | macOS 는 한글·일본어·베트남어·유럽어를 쪼개서 저장합니다. **영어만 쓰는 개발자는 절대 안 겪는 버그**라 놓치기 쉽습니다 |
| 낱말 경계 | 중국어·일본어는 띄어쓰기가 없습니다 |
| 이름 순서 | 한중일은 성이 먼저, 서양은 이름이 먼저 |
| 짧은 낱말의 위험 | '안' 을 넣었더니 '제안서' 가 '제서' 가 됐습니다 |

#### 실제로 겪은 사고 — NFD

```
파일시스템:  스크린샷  (ㅅ+ㅡ+ㅋ+ㅡ+ㄹ+ㅣ+ㄴ+ㅅ+ㅑ+ㅅ, 10글자)
정규식:      스크린샷  (4글자)
             → 안 맞음
```

결과: 스크린샷 265장이 한 가족으로 묶여 회수량이 3.1GB 부풀려졌고,
한글 이름 비율이 **0.4%로 세어졌습니다 (실제 6%)**.

> **규칙: 파일시스템에서 이름을 읽는 순간 NFC 로 바꾼다.**
> 그 뒤로는 프로그램 안에서 전부 NFC 만 돈다.

제품(2·3편)에서는 더 심각합니다 —
**한글로 검색해도 안 나오고, 되돌리기가 파일을 못 찾습니다.**

---

### 3-6. 개발 문화 (1편에서 그대로 계승)

**이 프로젝트의 버그는 대부분 "고쳤다고 생각했는데 안 고쳐진" 종류였습니다.**
그래서 규칙을 문서에만 적지 않고 테스트로 박습니다.

| | |
|---|---|
| **selftest** | 모든 변경 후 실행. 삭제 코드가 정해진 파일 밖에 생겼는지, 만들어 놓고 안 부르는 함수가 있는지, `except: pass`가 돌아왔는지 검사 |
| **더미 모드 `--demo`** | 화면 작업 시 디스크를 읽지도 쓰지도 않음. **진짜 스캔으로 화면을 시험하지 않습니다** |
| **버전 표시** | 화면과 터미널에 `v##`. 어긋나면 경고 |
| **`tools/check_ui.py`** | 디자인 시안을 넣기 전 검사 (id·class·script·미리보기 전용 코드) |
| **룰은 YAML로** | 판단 기준이 바뀌면 코드가 아니라 규칙 파일을 고침 |

**3·2편에 추가할 selftest 항목:**
- `core/`가 `probes/`의 내부 구조를 직접 들여다보지 않는가 (경계 침범)
- 이동/삭제 코드가 `mover.py` / `purge.py` 밖에 생기지 않았는가
- 참조 그래프 검사가 실제로 호출되는가 (만들어 놓고 안 부르는 것 방지)
- 흔한 이름 토큰(`안` 같은 두 글자 한국어)이 정규화에 들어오지 않았는가

### 3-7. 디자인 (DESIGN_SYSTEM.md 계승)

같은 뼈대에 **`--accent` 다섯 줄만 바꿔** 새 제품이 됩니다.

```
1편  보라  #6C5DF0
2편  ?     "질서" 계열 — 초록(#1F7A5C) 후보
3편  ?     별도
```

바꿀 것은 강조 다섯 줄 + `.brandmark` 아이콘 + 제목 문구뿐입니다.
그게 안 되면 어딘가에 색이 박혀 있는 것입니다.

**옛 색 이름(`--teal`, `--violet`)은 스크립트가 쓰므로 이름은 남기고 값만 연결합니다.**

---

## 4. 기술 — 어떻게 구현하나

### 4-1. 증거 사다리 (가장 중요)

```
1순위  계보                XMP · C2PA                    검증됨
2순위  생성 파라미터        PNG 텍스트 청크 (SD/ComfyUI)   검증됨
3순위  ★ 보편 지문         지각 해시(2D) · 기하 지문(3D)   측정값
4순위  파일명 + 시각 + 폴더                                추측
보조   옆에 내보낸 결과물                                  보조
```

**설계 원칙:**
> 포맷별 파서는 **정확도를 올리는 장치**이지 **동작의 전제가 아닙니다.**
> 아무 파서도 안 붙는 파일도 3순위로 묶여야 합니다.

**"옆에 jpg가 있다"를 보조로 강등한 이유:**
① 나중 버전에서 이름 안 바꾸고 재내보내기 ② 클라이언트 확인용 중간 내보내기
③ 최종은 PDF로 입고 ④ 내보내기를 데스크탑에 함 ⑤ 슬랙·메일로 바로 전송

특히 ②·④가 흔하고, **파일명 관리가 엉망인 사람일수록 ④가 심합니다.**

### 4-2. 계보 (XMP) — 실측으로 검증됨

어도비 파일은 XMP라는 평문 XML을 품고 다닙니다.

| 항목 | 뜻 |
|---|---|
| `xmpMM:OriginalDocumentID` | **최초 원본 ID. "다른 이름으로 저장"해도 안 바뀜** |
| `xmpMM:DerivedFrom` | 어느 파일에서 갈라졌나 |
| `xmpMM:History` | 저장 이벤트 목록 → **저장 횟수 = 진척도의 직접 측정값** |
| `xmp:CreatorTool` | 어느 프로그램 |

**바이트로 찾으므로 포맷을 안 가립니다.** psd·ai·indd·pdf 전부. 앞뒤 3MB만 읽어 2GB 파일도 즉시.

**`p0_survey.py` 시험 결과:**

```
같은 가족 5개 (이름은 제각각)
  ㅁㄴㅇㄹ.psd / 포스터초안.psd / 포스터_최종.psd / 제발제발.psd / 포스터_진짜최종_찐막.psd

파일 이름으로 묶기   →  3개  (ㅁㄴㅇㄹ, 제발제발 놓침)
계보로 묶기          →  5개 전부  ★
```

### 4-3. C2PA — AI 생성물

2026년 현재 주요 AI 툴이 전부 기본 탑재: Gemini·Imagen, Firefly, DALL-E·ChatGPT, Midjourney.
카메라도 서명합니다 (Sony·Nikon·Leica·Canon·Samsung·Pixel).

**한계:** 소셜 플랫폼이 업로드·트랜스코딩 과정에서 메타데이터를 벗겨냅니다.
카톡·인스타·슬랙을 거친 파일은 이미 벗겨져 있습니다.
→ **그래서 3순위(보편 지문)가 선택이 아니라 필수입니다.**

### 4-4. 보편 지문

**2D — 지각 해시**
```
QuickLook 썸네일  →  dHash  →  해밍 거리  →  가족
```
macOS는 앱이 설치돼 있으면 그 앱의 플러그인으로 썸네일을 만듭니다.
`.afdesign`·`.procreate`·`.sketch`·`.blend`가 전부 커버됩니다. **벤더 코드에 무임승차.**

*한계: 겉보기가 비슷한지만 봅니다. 초안과 최종이 완전히 다르면 못 묶습니다.*

**3D — 기하 지문**
```
정점 수 · 면 수 · 객체 수 · 바운딩 박스 · 재질 수
```
```
part_v1.stl   삼각형 24,819 · 45.2×30.1×12.8
part_v2.stl   삼각형 24,903 · 45.2×30.1×12.8   →  같은 모델의 수정본
handle.stl    삼각형  3,102 · 12.0× 8.0×80.0   →  다른 모델
```

### 4-5. 완성 단계 신호

**레이어 개수는 함정입니다.** 최종본일수록 병합·평탄화해서 레이어가 줄어듭니다.
`최종_인쇄용.psd`가 레이어 1개인 경우가 흔합니다.

| 포맷 | 완성 신호 | 강도 |
|---|---|---|
| PSD | **RGB → CMYK 전환** | ★★★ (입고 직전에만 함) |
| 전부 | **저장 횟수 (`History` 길이)** | ★★★ |
| PSD | 레이어 그룹 정리, `레이어 1` 비율 | ★★ |
| PSD | 텍스트가 로렘입숨 → 실제 문구 | ★★ |
| AEP | 렌더 큐 항목 · 출력 모듈 설정 | ★★★ |
| Premiere | 마커 없음 + 컷 수 안정, 오디오 믹스 존재 | ★★ |
| docx | 변경 추적 꺼짐 · 주석 없음 | ★★ |

**공통 구조: 실험 흔적이 사라지고 출력 준비 흔적이 나타난다.**

### 4-6. 코드 구조

```
core/                    포맷을 모름. 증거 등급과 stage 점수만 앎
  finding.py             공통 그릇 (1편에서 계승, 필드 추가)
  protector.py           관문 (1편 계승, 목록 확장)
  purge.py               삭제 (1편 계승 — 3편에서 사용)
  quarantine.py          vault (1편 계승 — 3편에서 사용)
  mover.py               이동 + journal (2편 신규)
  refgraph.py            참조 그래프 (2·3편 공용)
  cluster.py             가족 묶기
  memory.py              개인화 학습 (1편 계승)

probes/                  포맷별 어댑터. 정확도를 올리는 장치
  phash.py       ★ 보편 2D
  geomprint.py   ★ 보편 3D
  xmp.py           어도비
  c2pa.py          AI 생성물 · 카메라
  pngmeta.py       SD · ComfyUI
  stepmeta.py      STEP 평문 헤더
  psd.py           포토샵 내부
  blendfile.py     .blend
  zipfmt.py        Sketch · Procreate · Krita · OOXML

rules/                   판단 기준 (YAML. 코드와 분리)
```

**데이터 그릇:**
```
Work
  경로 · 크기 · 시각
  lineage      original_id · derived_from · save_count
  stage        완성 단계 점수 0~1        ← core는 CMYK도 렌더큐도 모름
  evidence[]   (종류, 등급, 사람이 읽을 문장)
  fingerprint  phash 또는 geomprint
  thumbnail
  raw{}        포맷별 원시값 — core는 안 들여다봄
```

**추상화 규칙:**
> 추상화는 **두 번째 사례가 나왔을 때** 뽑습니다.
> 이번엔 처음부터 둘입니다 — `phash`(포맷 무관)와 `xmp`(포맷 특정).
> 성격이 정반대인 둘을 맞춘 경계는 이미 검증된 것이라 세 번째를 붙일 때 안 깨집니다.
> **세 번째를 상상해서 미리 만들지 마세요.**

### 4-7. 규모

홈 폴더 전체는 파일 30만~100만 개가 흔합니다. `os.walk`로는 수십 분.
→ **Spotlight(`mdfind`/`NSMetadataQuery`)** 를 씁니다. 메타데이터도 딸려옵니다.
결과는 SQLite에 캐시합니다.

---

## 5. 3편 — 기능 명세

### 5-1. 화면

```
┌─────────────────────────────────────────────┐
│  포스터 작업 · 15개 버전 · 28 GB             │
├─────────────────────────────────────────────┤
│  [썸네일 15장을 시간순으로 가로 배치]         │
│   ↑                    ↑              ↑     │
│  3월 2일             3월 8일        3월 9일  │
│                     CMYK 전환 ★              │
│                     저장 31회                │
│                                             │
│  "3월 8일에 인쇄용으로 바꾸셨어요.            │
│   그 뒤 버전은 색만 조금 달라요."             │
│                                             │
│  ☑ 3월 8일 것 남기기                        │
│  ☐ 나머지 14개 → 기록으로 남기고 정리 (26GB) │
└─────────────────────────────────────────────┘
```

**축이 "그룹"이 아니라 "시간"입니다.** 같은 데이터인데 시간축만이 기억을 되살립니다.

**브리핑은 상태가 아니라 변화를 보여줍니다.**
```
❌  레이어 47개 · 그림자 3 · CMYK · 저장 23회      → 아무것도 안 떠오름
✅  3월 8일 → 9일 사이에 색을 CMYK로 바꾸셨고,
    텍스트를 전부 교체하셨어요                     → "아, 입고 직전이었지"
```

**목적은 정보 전달이 아니라 기억 되살리기입니다.**
"이게 최종입니다"가 아니라, 그때 무슨 일이 있었는지 떠올리게 해서 사용자가 판단하게 하는 것.

### 5-2. 기록으로 남기기 (핵심 발명)

```
지금:  poster_v1.psd … 15개 = 28 GB
이후:  poster_채택본.psd     2.1 GB   원본 유지
       poster_기록.pdf       4 MB     썸네일 15장 + 날짜 + 변화 요약
       ────────────────────────────
       확보 26 GB, "그때 뭐였는지"는 남음
```

**디자이너가 중간 버전을 못 지우는 건 비합리적인 게 아닙니다.**
"그때 그 파란색이 더 나았는데" 하는 순간은 실제로 옵니다.
그러니 "지우세요"라고 설득하지 말고, **세 번째 선택지**를 주는 것입니다.

조사한 어느 도구도 이걸 안 합니다. 전부 "지운다 / 안 지운다" 이분법입니다.

### 5-3. CleanMyMac과의 차이

```
CleanMyMac의 유사:  같은 순간을 여러 번 찍음 → 14장은 실패작 → 지워도 손실 0
                   판단: "어느 게 잘 나왔나"  (지금 보면 앎)

3편의 유사:        같은 작업의 시간적 진화 → 전부 의도적 결정의 산물
                   판단: "어느 게 채택됐나"  (기억해야 앎)
```

**연사 사진은 미적 판단이고, 디자인 버전은 역사적 사실입니다.**

그리고 CleanMyMac의 Similar Images는 사진 앱 밖의 이미지를 **휴지통도 안 거치고 영구 삭제**합니다.
"30일은 안전합니다"가 그대로 차별점이 됩니다 — 이미 만들어 놓은 기계니까요.

### 5-4. 포맷 전략

| 계층 | 무엇 | 대상 |
|---|---|---|
| 1. 가족 묶기 | 이름·크기·시간·지문 | **모든 파일** |
| 2. 완전 중복 | 해시 일치 | **모든 파일** (앞세우진 않음) |
| 3. 눈으로 비교 | 썸네일 | 이미지·PSD·PDF·영상 프레임·3D |
| 4. 속까지 비교 | 레이어·기하·텍스트 | 포맷별 |

**용량 가치가 포맷마다 다릅니다:**

| 포맷 | 15개 정리 시 | 파는 것 |
|---|---|---|
| **PSD/PSB** | **~28 GB** | 용량 + 혼란 |
| AI · INDD | ~2 GB | 혼란 (용량 보조) |
| **AEP · prproj** | **~0.5 GB** | **혼란만** |
| docx · pptx | ~0.1 GB | 혼란만 |

**영상은 프로젝트 버전 정리로 용량이 안 나옵니다.**
영상의 용량은 ① 렌더·프록시·캐시 ② **어느 프로젝트도 안 쓰는 원본 클립** 에 있고,
②는 버전 정리가 아니라 참조 그래프의 응용입니다.

### 5-5. 3D 전략

**① 자동 백업 파일 — 가장 쉽고 가장 큰 승리**

| 툴 | 자동 생성물 |
|---|---|
| Blender | `.blend1` `.blend2` `.blend3` |
| Rhino | `.3dmbak`, autosave |
| AutoCAD | `.bak` `.sv$` `.dwl` `.dwl2` |
| SolidWorks | `~$파일` |

사용자가 만든 게 아니라 프로그램이 만든 것 → 판단 쉬움, 용량 큼(원본 크기 × N), 위험 낮음.
**1편의 ②구간(다시 생기는 것)에 가장 가깝습니다.**

**② 썸네일이 됩니다**
Blender는 기본으로 `.blend`에 미리보기를 저장하고, 설치 시 macOS QuickLook 확장을 함께 설치합니다.

**③ STEP이 3D판 XMP입니다**
ISO 10303-21 표준, 평문 텍스트. 헤더에 작성 시각·작성자·생성 프로그램이 그대로 들어 있습니다.

**④ 어셈블리 = 참조 지옥이자 강력한 증거**
파트를 옮기면 어셈블리가 열리지도 않습니다. 반대로
"이 파트를 참조하는 어셈블리가 없다" = 고아 파일, 확실 등급.

**⑤ 다운로드 에셋 중복 — 3D에서는 완전 중복도 앞세울 만합니다**
Quixel·BlenderKit·Poliigon 에셋이 프로젝트마다 복사됩니다. 8K 텍스처 세트 × 5 = 수십 GB.

**포맷별 실현 가능성**

| 포맷 | 썸네일 | 메타 | 기하 지문 | 난이도 |
|---|---|---|---|---|
| **STEP** | ❌ | ✅✅ 평문 | ✅ | **낮음** |
| **STL** | ❌ | △ | ✅✅ | **낮음** |
| **OBJ** | ❌ | △ | ✅✅ | **낮음** |
| .blend | ✅ | △ | ✅ | 중 |
| .3dm | ✅ | ✅ OpenNURBS 공개 | ✅ | 중 |
| .skp | ✅ | SDK | ✅ | 중 |
| .f3d | △ | zip | △ | 중 |
| .dwg / .sldprt | ❌ | ❌ | ❌ | **하지 않음** |

### 5-3b. 중복은 버전 정리를 못 해서 생긴 결과입니다

한때 "완전 중복은 우리 상품이 아니다, CleanMyMac 영역" 이라고 정했다가
**되돌렸습니다.** 실측이 그 구분이 틀렸다는 걸 보여줬습니다.

```
완전 중복 1,021개 중
  '복사'·'copy'·'사본' 이 붙은 것        183개
  같은 촬영 폴더 안                     494개 · 169개
```

조언 예시를 보면 더 명확합니다.

```
DSC09195 복사.psd  ↔  DSC09195.psd
이빨보정 복사.psd   ↔  이빨보정.psd
```

**버전을 만들려다 그냥 복사한 것**입니다. 원인이 같습니다.

```
버전을 못 만듦  →  일단 복사해 둠     →  완전 중복
              →  이름을 못 지음     →  '복사 2', '사본' 이 쌓임
              →  뭘 지울지 모름     →  둘 다 남김
```

**사용자 입장에서는 같은 문제의 다른 단계**입니다.
그러니 나누지 말고 한 흐름에서 **등급만 다르게** 보여줍니다 (5-3 등급 참조).

CleanMyMac 과의 차이도 그대로 유지됩니다 —
같은 중복을 찾아도 **왜 생겼는지 말해 주고, 30일 보관하고,
근거를 붙여 자동 선택**하는 것은 우리만 합니다.

---

### 5-4b. 특수상황 감지 — 열거하지 말고 튀는 걸 찾기

사용자가 실행 버튼을 누르는 순간은 이렇습니다.

> *"어? 내 맥북 진짜 이것저것 섞여 있는데 얘가 그 맥락을 전부 짚어내네.
>  똑똑하다. 믿고 맡겨도 되겠어."*

**3-1 화면은 정보 제공이 아니라 신뢰 획득 장치입니다.**
그리고 신뢰의 재료는 정확한 분석이 아니라 **"내 사정을 알아봐 줬다"** 는 느낌입니다.

#### 원리

특수상황은 종류가 무한합니다 — 유학원 강사, 사진 스튜디오, 번역가, 인쇄소…
감지기를 하나씩 만들면 절대 못 따라갑니다.

```
❌  학생 파일 감지기 + 클라이언트 감지기 + 촬영본 감지기 + …
✅  이 사람의 다른 폴더와 비교해 튀는 폴더를 찾고, 왜 튀는지 설명하게 하기
```

**기준선을 이 사람 자신으로 삼으면 세상의 모든 직업을 몰라도 됩니다.**

*(실제로 '학생 이름 감지기'를 성씨 목록으로 만들었다가, 한 직업에만 통한다는
걸 알고 되돌린 적이 있습니다. 과하게 만든 게 오히려 덜 작동했습니다.)*

#### 신호 — 전부 이름의 '뜻'이 아니라 '모양'

| 신호 | 튀면 무슨 뜻일 수 있나 |
|---|---|
| **생성일 = 수정일 비율** | **받아만 놓고 한 번도 안 연 것** ★ |
| 이름 길이가 짧음 | 목록·명단 (이름만 적은 것) |
| 이름 길이가 고름 | 규칙적으로 붙인 것 |
| 수정일이 한 시기에 몰림 | 한 프로젝트·한 학기 |
| 확장자가 한 종류만 | 수집물·받은 것 |
| 원본↔내보낸 것 쌍이 없음 | 내가 만든 게 아님 |
| 이 폴더에서만 쓰는 낱말이 많음 | 밖에서 들어온 묶음 |
| 숫자로 시작하는 이름이 많음 | 날짜 규칙을 쓰던 시기 |

**마지막 줄이 기획서 1-1의 "정리하려다 중도 포기"를 직접 감지합니다.**
2021년엔 날짜로 시작하다가 2023년부터 안 그러면, 그 시점에 포기한 것입니다.

#### 여기가 LLM의 진짜 자리입니다

```
입력 (파일 이름 없음):
  폴더 #3  파일 47개 · 이름 평균 3.1글자 · 생성=수정 61%
           한 시기에 몰림 · 확장자 psd 단일 · 내보낸것 쌍 없음
출력:
  "다른 분들 작업을 받아 보관하시는 것 같아요.
   대부분 한 번도 안 여신 상태네요. 여긴 건드리지 않을게요."
```

한 번 실행 · 입력은 통계 · 출력은 문장 — **소량 판단이라 LLM의 자리**입니다.
그리고 **모양만 보므로 파일 이름이 필요 없어**, "이름 붙은 데이터는 밖으로
안 나간다"는 원칙과 구조적으로 맞습니다.

#### 설계 제약

> **모르는 건 모른다고 해야 합니다.**

특수상황을 다섯 개 짚고 하나를 틀리면 사용자는 틀린 하나만 기억합니다.
확실한 것만 말하고 나머지는 *"이 폴더는 성격을 잘 모르겠어요"* 로 둡니다.

---

### 5-5. 소유 구분 — 내 작업 / 남의 작업

**미대 졸업생 상당수가 강사·외주를 겸합니다.** 그러면 디스크에
학생 포폴, 클라이언트 원본, 동료가 넘긴 파일이 섞입니다.

사용자는 이걸 압니다. 그래서 정리 앱을 켜면 이렇게 걱정합니다.

> *"내 것도 아닌 파일까지 다 섞어서 옮겨버리면 어떡하지"*

**이 걱정을 먼저 없애 주는 것이 페르소나 진단의 진짜 목적입니다.**
분석 결과를 보여주는 게 아니라, 실행 버튼을 누를 수 있게 만드는 장치입니다.

```
❌  "2023년 상반기에 psd 340개를 만드셨네요"        ← 그래서 뭐?
✅  "학생 작업물도 다루시는 것 같아요.
     그 파일들은 건드리지 않고 따로 모아 둘게요."    ← 안심하고 누름
```

**무엇으로 아는가 (증거 사다리):**

| 증거 | 등급 |
|---|---|
| **파일 안에 다른 사용자의 홈 경로가 박혀 있음** (`/Users/남/…`) | **검증됨** ★ |
| **한 폴더에 사람 이름 형태의 파일이 여럿** (명단) | **검증됨** ★ |
| XMP 작성자·저장 이력이 나와 다름 | 검증됨 |
| 다운로드 출처(`kMDItemWhereFroms`)가 메일·드라이브 | 검증됨 |
| 이름의 강한 단서 — 학생·첨삭·포폴·외주·클라이언트·납품 | 보조 |
| 이름의 약한 단서 — 원본·시안·과제 (내 작업에도 붙음) | 참고 |

**둘째 줄이 실전에서 더 자주 걸립니다.** 강사들은 접두어 없이 이름만 적는 경우가
많습니다 — `김민준.psd`, `김민준_최종.psd`, `이서연.psd`. 이러면 '학생' 같은
단서 낱말이 아예 없습니다.

```
파일 하나만 보면   '표지.psd' 인지 '김민준.psd' 인지 헷갈림
폴더를 통째로 보면  서로 다른 사람 이름 30개 = 명단. 헷갈릴 여지 없음
```

빈도로는 못 잡습니다. 한 사람당 파일이 여러 개면 오히려 빈도가 높아져
'흔한 낱말'로 분류됩니다. **이름의 생김새**(성씨 + 2~4글자)로 잡아야 합니다.

첫 줄도 결정적입니다. PSD의 링크 이미지 경로, INDD 링크, 프로젝트 소스 경로에
`/Users/누구/…`가 그대로 저장되는데, 그 '누구'가 나와 다르면 **다른 사람 컴퓨터에서
만들어진 파일**입니다. 이름이 아무리 엉망이어도 확실합니다.

**처분:** 남의 작업물은 버전 정리 대상에서 **빼고** 따로 모읍니다.
지우지도, 옮기지도 않고 "여기 이만큼 있습니다"만 알려 줍니다.

---

### 5-6. 버전 ≠ 탐색 — UI가 달라야 합니다

같은 "유사 파일"이라도 성격이 둘로 갈립니다.

| | 무엇 | 판단 | UI |
|---|---|---|---|
| **버전** | 같은 작업의 시간적 진화 | "어느 게 채택됐나" (기억해야 앎) | **시간축** |
| **탐색** | 여러 안을 동시에 시도 | "어느 게 마음에 드나" (지금 보면 앎) | **격자** |

AI 생성물(Midjourney·ComfyUI 배치), 연사 사진, 시안 A/B/C안이 **탐색** 쪽입니다.
`ComfyUI` 출력 수천 장을 시간축에 늘어놓으면 아무 의미가 없습니다.

**묶는 방법도 다릅니다:**
```
버전:  계보(OriginalDocumentID) · 저장 이력
탐색:  같은 프롬프트 = 같은 시도 / 같은 시드 = 같은 이미지의 변주
```

---

## 5-B. 2편 — 화면 명세

원래 기획서(7단계)에 대한 결론입니다.

| 단계 | 결론 |
|---|---|
| 1~2 홈·스캔 | 유지. 스캔이 길면 **진행 중에 찾은 것을 보여주기** |
| **3-1 페르소나 분석** | 유지하되 **요약 3줄 + 펼치기.** 관문으로 쓰면 안 됩니다 |
| | ★★ **진짜 목적은 신뢰 획득입니다.** 분석 보고서가 아니라 "내 사정을 알아봐 줬다"는 느낌 |
| | ★ **사용자가 고칠 수 있어야 합니다.** 틀린 페르소나는 신뢰를 즉사시킵니다 |
| | ★ 진짜 목적은 분석 표시가 아니라 **사용자에게 폴더 이름을 짓게 하는 것** |
| 3-2 top5 | "많이 한 작업"보다 **"최근에도 하는 작업"** 이 정리에 유용 |
| 2-2 규칙 없는 파일 | 유지. **사용자가 자책하지 않는 문구로** |
| 2-3 선호 방식 추론 | ★ 페르소나 파트에서 **유일하게 계획에 실제로 쓰이는 정보.** 날짜형/프로젝트형/종류형 |
| 5-1 폴더 구조 시각화 | 핵심 화면. **before/after 대비** 필요 |
| **5-5 애매한 파일 보관함** | ★ **채택.** 1편 ④구간(기억나세요?)과 같은 발상 |
| | 이름 주의: "미분류" ❌ → **"나중에 같이 볼 것"** ✅ |
| 5-6 채팅 수정 | **무한 반복은 위험합니다.** 계속 고치다 지쳐서 실행을 안 누릅니다 |
| | 되돌리기가 완벽하면 **"일단 해보고 고치기"가 더 나은 UX** |
| 5-7 실행 | "한 달 백업" → **기한 없는 이동 장부**로 (3-2 참조) |
| 7-1 검색 | ★ **사실상 이 앱의 핵심.** 그래서 순서를 앞으로 뺐습니다 |
| 7-3 되돌리기 | 년월일시 분기점. journal이라 **무제한** |
| 상시 감시 | P12'. 규칙이 실전 검증된 뒤 |

### 5-2단계가 가장 위험합니다

"pc 사용 시작 ~ 1년 전까지의 파일을 최적화 계획대로 정리" —
**여기서 수만 개가 한 번에 움직입니다.** 참조 깨짐·클라우드 사고·부분 실패가 전부 여기서 터집니다.

→ **가장 오래된 연도 하나 → 확인 → 다음** 으로 단계적 실행.
사용자가 중간에 "어, 이건 아닌데"를 발견할 기회가 생깁니다.

### 애매한 파일의 정체

**애매해서 애매한 게 아니라, 폴더 하나로는 표현이 안 돼서 애매합니다.**

```
진짜최종_포스터.psd 는 무엇인가?
  → 클라이언트A 것 · 2023년 것 · 포스터 · 인쇄물
  그런데 폴더는 하나만 고를 수 있다
```

Eagle이 이미 진단한 문제고, 우리 해법은 **폴더 + macOS 태그**입니다 (9장 참조).

---

## 6. 단계

### P0 — 실측 (읽기 전용) ← **지금 여기**

`p0_survey.py`. 파일을 하나도 바꾸지 않습니다. 30개 항목을 재서 아래를 결정합니다.

```
계보만 찾아낸 파일 %     →  3편의 차별성이 있는가
3편 예상 회수량          →  2편 먼저냐 3편 먼저냐
손댈 수 있는 비율        →  2편의 범위 (홈 전체 vs 세 폴더)
규칙 vs LLM 정확도       →  로컬 모델이 필요한가
자동 백업 용량           →  3D 대응의 첫 기능이 확정되는가
```

**본인 맥 + 친구 4명.** 한 대로는 못 정합니다.

### 이후 — P0 결과에 따라 두 갈래

```
공통 기반 (어느 쪽이든 먼저)
  P1  인벤토리 DB          Spotlight → SQLite
  P2  검색 v1              메타데이터 필터 ("지난주 psd")
  P3  검색 v2              색상·치수·지문
  P4  쿼리 해석            자연어 → 필터. LLM 첫 결정 지점
  P5  검색 화면            ◆ 여기서 이미 배포 가능한 제품
```

**3편 갈래**
```
  P6   probes/phash + geomprint     보편 지문
  P7   probes/xmp                   계보
  P8   자동 백업 파일 탐지            3D 첫 기능 (가장 쉬움) — 세기만 함
  P9   probes/psd                   완성 단계 신호
  P10  refgraph                     참조 그래프 (안전 관문)
  P11  가족 묶기 + 시간축 화면
  P12  기록 PDF 생성
  ──────── 여기까지 읽기 전용 ────────
  P13  삭제 실행 (1편 vault 재사용)   한 개 → 열 개 → 전체
  P14  probes/c2pa · pngmeta · stepmeta · blendfile
```

**2편 갈래**
```
  P6'  페르소나 분석                 LLM 두 번째 결정 지점
  P7'  refgraph                     "옮기면 깨지는 것"
  P8'  안전 관문 2편 버전
  P9'  계획 dry-run                 이동 0건
  ──────── 여기까지 읽기 전용 ────────
  P10' mover + journal              한 개 → 열 개 → 전체
  P11' 정리 화면
  P12' 상시 감시 + 주간 추천
```

**3편은 P12까지, 2편은 P9'까지가 읽기 전용입니다.**
파일을 처음 건드리는 건 P13 / P10'입니다.
1편이 아홉 단계를 읽기 전용으로 가서 오탐 7건을 잡은 것과 같은 구조입니다.

### 왜 검색이 먼저인가

1. 검색이 되면 **정리가 조금 틀려도 용서됩니다.** 못 찾는 게 유일한 진짜 공포니까요
2. 읽기 전용이라 파일을 하나도 안 옮기고 만듭니다
3. 인벤토리 DB가 검색과 정리의 공통 기반입니다
4. **정리 없이 검색만으로도 이미 쓸 만한 앱입니다.** 중간에 멈춰도 제품이 됩니다
5. **검색이 못 찾는 파일 = 분류도 못 하는 파일.** 분류 엔진의 무료 테스트베드가 됩니다

*단, 검색을 단독 상품으로 팔지는 마세요.* 애플이 macOS 26에서 Spotlight에 크게 투자했습니다.
"정리 + 정리 후 추적"의 일부로 두면 안 부딪힙니다.

---

## 7. P0 측정 항목 (30개)

### 규모·환경 (1~15)
| # | 항목 |
|---|---|
| 1 | 파일 수 — 홈 전체 vs 세 폴더 |
| 2 | `mdfind` 소요 시간 |
| 3 | 클라우드 폴더 비중 |
| 4 | 앱 라이브러리 번들 개수·용량 |
| 5 | **★ 실제로 손댈 수 있는 파일 %** |
| 6 | 마지막 사용일 분포 |
| 7 | 참조 파일 개수 |
| 8 | 이름 품질 분포 |
| 9 | 중복 파일 비율 |
| 10 | 한국어 파일명 비율 |
| 11 | Intel / Apple Silicon |
| 12 | macOS 버전 (26 이상 비율) |
| 13 | 오사우르스 연결 여부 |
| 14 | 한국어 LLM 분류 정확도 (수동) |
| 15 | **★ 규칙 vs LLM 정확도 비교** (수동) |

### 계보·2D (16~25)
| # | 항목 |
|---|---|
| 16 | XMP 블록 존재 비율 |
| 17 | **★★ 계보로 묶인 가족 vs 이름으로 묶인 가족** |
| 18 | 내장 썸네일 추출 성공률 |
| 19 | `History` 평균 길이 (저장 횟수) |
| 20 | PSD 중 CMYK 비율 |
| 21 | **★ QuickLook 썸네일 생성 성공률** (포맷별) |
| 22 | PNG 생성 파라미터 존재 비율 |
| 23 | C2PA 매니페스트 존재 비율 |
| 24 | 비어도비 작업 파일 비율 |
| 25 | 지각 해시로만 묶이는 가족 수 |

### 3D (26~30)
| # | 항목 |
|---|---|
| 26 | **★ 자동 백업 파일 개수·용량** |
| 27 | 3D 확장자 분포 |
| 28 | STL / OBJ / STEP 비율 |
| 29 | `.blend` 썸네일 추출 성공률 |
| 30 | Fusion 로컬 캐시 용량 |

### 소유 (31)
| # | 항목 |
|---|---|
| 31 | ~~남의 작업물이 섞여 있는가~~ — **보류.** 순서 판단에 안 쓰이고, 표본 기준이라 실제보다 낮게 나옵니다. `--ownership` 으로만 실행 |
| 32 | **★★ 튀는 폴더** — 이 사람의 다른 폴더와 비교한 이상치 |

---

## 8. 아직 안 정한 것

| | 언제 정하나 |
|---|---|
| **2편 먼저냐 3편 먼저냐** | P0 결과 |
| 2편 범위 (홈 전체 vs 세 폴더) | P0 #5 |
| LLM을 어디까지 쓸까 | P0 #15, 그리고 P4 |
| 클라우드 LLM 허용 여부 (페르소나 분석만) | P6' 도달 시 |
| 3편 가격·배포 형태 | 나중 |
| 4편 (가족 모드) 착수 여부 | 검색 완성 후 |

---

## 9. 경쟁 지도

| | 하는 것 | 안 하는 것 |
|---|---|---|
| **애플 (Spotlight)** | 찾기 | **정리** (구조적으로 안 함) |
| **Sortio** | 정리 | **규칙 생성** — 사용자가 줘야 함. B2B(변호사·중개인)로 이탈 |
| **Hazel** | 자동화 | 규칙을 짤 줄 알아야 함 |
| **Claude Cowork** | 다 함 | **말할 줄 알아야 함.** 되돌리기·안전 관문 없음 |
| **CleanMyMac** | 중복·유사 사진 | **작업 원본 버전**, 기록 남기기, 30일 보관 |
| **Eagle** | 가상 분류 (한 파일이 여러 폴더에) | 자체 라이브러리 감옥 — 우리 타깃엔 재앙 |

**Eagle에서 배울 것:** 가상 분류의 발상. 단 구조는 안 가져오고 **macOS 태그**로 구현.
```
물리적 위치 = 폴더 하나 (Finder에서 볼 것)
논리적 위치 = macOS 태그 여러 개 (검색으로 도달)
```
우리 앱을 지워도 태그가 파일에 남습니다.

**빈자리:**
> "뭘 원하는지 말 못 하는 사람에게, 규칙을 대신 만들어 주고, 안전하게 되돌릴 수 있게."

애플은 안 할 일이고, 경쟁앱은 안 하는 일이고, Cowork는 못 하는 일입니다.
