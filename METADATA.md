# 메타데이터 총정리 — 무엇을 읽고, 그걸로 무엇을 아는가

`p0_survey.py` 가 단계마다 실제로 무엇을 읽는지, 그 값이 제품에서 어떤 판단에
쓰이는지 정리했습니다. 순서는 실행 순서입니다.

**등급 표기** (`CLAUDE.md` 3항)

| | |
|---|---|
| **검증됨** | 이 컴퓨터에서 직접 확인한 것. 단독으로 '확실' 판정 가능 |
| **측정값** | 계산으로 얻은 것. 정확하지만 해석이 필요 |
| **보조** | 참고. 다른 증거와 겹칠 때만 의미 |
| **추측** | 단독으로는 결론 금지 |

---

## 1단계 · 환경 확인

**읽는 것** — 파일이 아니라 시스템 상태

| 값 | 출처 | 등급 |
|---|---|---|
| CPU 종류 (Intel / Apple Silicon) | `sysctl machdep.cpu.brand_string` | 검증됨 |
| macOS 버전 | `sw_vers` | 검증됨 |
| 메모리 | `sysctl hw.memsize` | 검증됨 |
| Spotlight 켜짐 여부 | `mdutil -s /` | 검증됨 |
| `mdfind` 응답 시간 | 실제 한 번 돌려 잼 | 측정값 |
| 1337 포트 응답 | `curl` | 검증됨 |

**알아내는 것**

- **로컬 LLM 을 쓸 수 있는가** — Intel 이면 MLX·오사우르스 불가. 규칙 엔진 중심 설계가 강제됩니다
- **Apple Foundation Models 를 공짜로 쓸 수 있는가** — macOS 26 이상이면 모델 다운로드 0MB
- **2편 스캔 화면을 몇 초짜리로 짤 것인가** — `mdfind` 가 2초면 즉시 결과 화면, 3분이면 진행 화면이 필요합니다
- **메모리 8GB 사용자를 어떻게 다룰 것인가**

---

## 2단계 · 파일 훑기

**읽는 것** — 파일을 열지 않습니다. 디렉터리 정보만 봅니다.

| 값 | 출처 | 등급 |
|---|---|---|
| 경로 · 이름 · 확장자 | `os.scandir` | 검증됨 |
| 크기 | `stat.st_size` | 검증됨 |
| 수정 시각 | `stat.st_mtime` | 검증됨 |
| **생성 시각** | `stat.st_birthtime` (**macOS 전용**) | 검증됨 |
| 심볼릭 링크 여부 | `is_symlink` | 검증됨 |
| 클라우드 폴더 여부 | 경로 이름 대조 | 검증됨 |
| 앱 라이브러리 번들 여부 | 확장자 대조 (`.photoslibrary` 등) | 검증됨 |
| 권한 오류 | `PermissionError` 를 셈 | 검증됨 |

**알아내는 것**

| | |
|---|---|
| **실제로 손댈 수 있는 파일이 몇 %인가** ★ | 클라우드·번들·개발 폴더를 빼고 남는 비율. 40% 미만이면 홈 전체 스캔이 낭비입니다 |
| 규모 | 20만 개를 넘으면 2편은 Spotlight 없이는 불가능 |
| **생성 시각 ≠ 수정 시각** | 뒤에서 "받아만 놓고 안 연 파일" 판정에 쓰입니다 |
| 못 읽은 곳 | 전체 디스크 접근 권한이 없는 폴더. 숨기지 않고 셉니다 |

**한계 하나** — `st_mtime` 은 "마지막으로 **저장**한 때"이지 "마지막으로 **연**
때"가 아닙니다. 제품에서는 Spotlight 의 `kMDItemLastUsedDate` 를 써야
정확합니다. (5년 전에 만들었어도 지난달에 열었다면 살아 있는 파일입니다)

---

## 3단계 · 캐시·렌더 확인

**읽는 것** — 알려진 절대 경로에 폴더가 있는지, 있으면 용량만

```
~/Library/Caches/Adobe/Common/Media Cache      Premiere·AE 미디어 캐시
~/Library/Developer/Xcode/DerivedData          Xcode 빌드 산출물
~/Movies/Final Cut Backups                     Final Cut 백업
Previews.lrdata                                Lightroom 프리뷰
Adobe Premiere Pro Video Previews              Premiere 렌더
…
```

**알아내는 것**

- **다시 만들어지는 것이 몇 GB인가** — 1편의 ②구간과 같은 성격이라 판단이 쉽고 위험이 없습니다
- 영상 작업자에게는 이게 가장 큰 몫일 수 있습니다

**주의** — 이 폴더들은 `~/Library` 안에 있고, 훑기는 `Library` 를 통째로
건너뜁니다. 그래서 **절대 경로로 따로 확인**합니다. 한때 훑기에 맡겼다가
캐시 용량이 0으로 나온 적이 있습니다.

---

## 4단계 · 버전 가족 묶기

**읽는 것** — 파일 이름만. 내용은 안 봅니다.

```
포스터_v1.psd        →  뼈대 '포스터'
포스터_최종.psd       →  뼈대 '포스터'
포스터_진짜최종.psd    →  뼈대 '포스터'
IMG_4821.jpg        →  뼈대 'img 4821'  (카메라 접두어는 숫자를 남김)
```

버전 표시를 걷어낸 뒤 **같은 폴더 · 같은 확장자**끼리 묶습니다.

**알아내는 것**

| | 등급 |
|---|---|
| 버전 가족 수 · 가족에 속한 파일 수 | **추측** |
| **가장 큰 것만 남기면 회수 가능한 용량** ★ | **추측** |

**전부 추측 등급입니다.** 이름은 못 믿습니다. 이 결과는 그 자체가 목적이
아니라, 뒤의 **계보(XMP)와 비교하기 위한 대조군**입니다.

---

## 5단계 · 완전 중복 검사

**읽는 것** — 크기가 같은 파일들의 앞 64KB + 뒤 64KB + 크기

크기가 다르면 내용도 다르므로, 크기가 같은 것끼리만 해시합니다.

**알아내는 것**

- 내용이 100% 같은 파일이 몇 GB인가 — **측정값**
- ※ 이건 우리 상품이 아닙니다. CleanMyMac 등이 이미 잘 합니다.
  다만 규모 기준선이고, **3D 에셋(같은 텍스처가 프로젝트마다 복사)** 에서는
  예외적으로 앞세울 만합니다

---

## 6단계 · 자동 백업 찾기

**읽는 것** — 확장자와 이름 규칙. 그리고 **원본이 옆에 있는지**

| 패턴 | 만든 프로그램 |
|---|---|
| `.blend1` `.blend2` `.blend3` | Blender |
| `.3dmbak` | Rhino |
| `.bak` `.sv$` `.dwl` | AutoCAD |
| `~$파일` | Office |

**알아내는 것**

| | 등급 |
|---|---|
| 프로그램이 스스로 만든 백업이 몇 GB인가 | **검증됨** |
| **원본이 옆에 있는가** ★ | **검증됨** |

**두 번째가 결정적입니다.** 원본이 있어야 안전하게 지웁니다.
원본이 없는 `.blend1` 은 사실상 유일본이라 지우면 못 되돌립니다.

사용자 의도가 없는 파일이라 **판단이 쉽고 위험이 낮고 용량이 큽니다.**
3D 대응의 첫 기능 후보입니다.

*(원본 경로 계산에 실수가 있었습니다 — `part.3dmbak` 에서 확장자를 통째로
떼면 `part` 가 되는데 원본은 `part.3dm` 입니다. 5개 중 2개만 잡히다 고쳤습니다.)*

---

## 7단계 · 3D 파일 세기

**읽는 것** — 확장자만

| 분류 | 예 | 왜 나누나 |
|---|---|---|
| 네이티브 | `.blend` `.3dm` `.skp` `.f3d` | 썸네일이 들어 있음 |
| 교환 포맷 | `.stl` `.obj` `.step` | 텍스트·단순 바이너리라 파싱이 쉬움 |

**알아내는 것** — 3D 대응을 어디부터 시작할지. 교환 포맷이 많으면 거기부터가
훨씬 쉽습니다.

---

## 8단계 · 참조·한글·비어도비 세기

**읽는 것** — 확장자와 이름의 글자 종류

| 값 | 알아내는 것 | 등급 |
|---|---|---|
| **외부 파일을 참조하는 포맷의 개수** | 참조 그래프를 언제 만들어야 하는가. `.psd`(링크 이미지) `.indd` `.prproj` `.blend` `.sldasm` 등 | 검증됨 |
| 한글이 든 이름의 비율 | 이름 정규화·쿼리 해석을 한국어 기준으로 설계할지 | 검증됨 |
| 비어도비 작업 원본 비율 | XMP 계보에만 의존할 수 없는지. `.afdesign` `.procreate` `.sketch` `.blend` 등 | 검증됨 |

**첫 줄이 중요합니다.** 참조를 가진 파일이 많으면, 이동·삭제 기능보다
**참조 그래프를 먼저** 만들어야 합니다. 그 대상을 옮기면 이 파일들이 깨집니다.

---

## 9단계 · `.blend` 머리말

**읽는 것** — 파일 앞 12바이트 + 앞 1MB

```
BLENDER-v405…      매직 문자열 + 포인터 크기 + 버전
…TEST…            썸네일 블록이 있는지
\x1f\x8b           gzip 압축 저장이면 여기서 멈춤
```

**알아내는 것**

- **`.blend` 에서 썸네일을 꺼낼 수 있는가** — Blender 는 기본으로 미리보기를
  파일에 넣고, 설치 시 macOS QuickLook 확장도 함께 깝니다
- Blender 버전 분포

---

## 10단계 · 낱말 빈도

**읽는 것** — 파일 이름을 낱말로 쪼갠 것

```
포스터_최종_v2.psd  →  ['포스터', '최종', 'v2']
```

**알아내는 것** — 이 사람이 무슨 일을 하는지의 재료 (**추측** 등급)

**⚠ 이 절에는 실제 낱말이 그대로 나옵니다.** 학생 이름만 적어 두는 사람이라면
이름이 여기 올라옵니다. 보고서를 남에게 줄 일이 생기면 이 절부터 확인하세요.

---

## 11단계 · 폴더 모양 (항목 32)

**읽는 것** — 앞 단계들에서 이미 모은 값을 폴더 단위로 집계

폴더마다 여덟 가지를 잽니다. **전부 이름의 '뜻'이 아니라 '모양'입니다.**

| 신호 | 계산 | 튀면 무슨 뜻일 수 있나 |
|---|---|---|
| `untouched` | 생성 시각 ≈ 수정 시각인 비율 | **받아만 놓고 한 번도 안 연 것** ★ |
| `namelen` | 이름 길이 평균 | 짧으면 목록·명단 |
| `namevar` | 이름 길이의 흔들림 | 작으면 규칙적으로 붙임 |
| `timespan` | 수정일이 퍼진 기간 | 좁으면 한 프로젝트·한 학기 |
| `extmix` | 최빈 확장자 비율 | 1이면 한 종류만 = 수집물 |
| `pairless` | 원본↔내보낸 것 쌍이 없는 비율 | 높으면 내가 만든 게 아닐 수 있음 |
| `exclusive` | 이 폴더에서만 쓰는 낱말 비율 | 높으면 밖에서 들어온 묶음 |
| `digitstart` | 숫자로 시작하는 이름 비율 | 날짜 규칙을 쓰던 시기 |

그다음 **이 사람의 다른 폴더와 비교**해 튀는 곳을 고릅니다 (중앙값·MAD 기준).

**알아내는 것 — 특수상황**

직업을 열거하지 않고 찾습니다. 기준선이 이 사람 자신이라 세상의 모든 직업을
몰라도 됩니다.

```
❌  학생 감지기 + 클라이언트 감지기 + 촬영본 감지기 + …
✅  이 사람의 다른 폴더와 비교해 튀는 폴더를 찾고, 왜 튀는지는 LLM 이 설명
```

**여기가 2편 페르소나 화면의 실제 근거입니다.** 그리고 보내는 게 통계뿐이라
파일 이름이 밖으로 나가지 않습니다.

**마지막 줄이 기획서 1-1의 "정리하려다 중도 포기"를 직접 감지합니다.**
2021년엔 날짜로 시작하다가 2023년부터 안 그러면, 그 시점에 포기한 것입니다.

**⚠ 주의 셋**
- 폴더가 8곳 미만이면 '보통'의 기준이 없어 아무것도 안 잡힙니다
- `untouched` 는 macOS 에서만 됩니다 (생성 시각이 있어야 함)
- 최소 편차 바닥값이 없으면 오탐이 납니다 — 다른 폴더가 900~1000일로 촘촘하면
  825일짜리가 "한 시기에 몰림"으로 잡힙니다 (실제로 났던 오탐)

---

## 12단계 · 계보 (XMP) ★ 가장 중요

**읽는 것** — 파일 앞 3MB + 뒤 3MB 에서 XMP 블록을 바이트로 찾습니다

XMP 는 평문 XML 이라 **포맷을 안 가립니다.** psd·psb·ai·indd·pdf·jpg·tif 전부.
2GB 파일이어도 앞뒤만 읽으므로 즉시 끝납니다.

| 항목 | 뜻 | 등급 |
|---|---|---|
| **`xmpMM:OriginalDocumentID`** | **최초 원본의 ID. "다른 이름으로 저장"을 해도 안 바뀝니다** | **검증됨** ★ |
| `xmpMM:DocumentID` | 이 파일의 ID | 검증됨 |
| `xmpMM:DerivedFrom` | 어느 파일에서 갈라졌나 | 검증됨 |
| **`xmpMM:History` 길이** | **저장 횟수 = 이 파일을 얼마나 붙들고 있었나** | **검증됨** ★ |
| `xmp:CreatorTool` | 어느 프로그램으로 만들었나 | 검증됨 |
| `xmp:CreateDate` / `ModifyDate` | 파일시스템과 별개인 진짜 작업 시각 | 검증됨 |

**알아내는 것**

**① 이름을 안 믿고도 가족을 묶을 수 있습니다**

```
같은 OriginalDocumentID  =  같은 작업의 다른 버전
```

`ㅁㄴㅇㄹ.psd` 와 `제발제발최종.psd` 가 같은 작업의 3번째·11번째 버전이라는 걸
**이름이 아니라 혈통으로** 압니다.

시험에서 이름으로는 5개 중 3개만 묶였는데 계보로는 5개 전부 묶였습니다.

**② 저장 횟수가 진척도의 직접 측정값입니다**

3번 저장한 파일과 80번 저장한 파일은 다른 물건입니다.

**③ ★★ 이름 방식과 비교한 차이가 3편의 존재 이유입니다**

```
★★ 계보만 찾아낸 파일    ___ 개  ← 이름이 놓친 것
```

20%를 넘으면 CleanMyMac 도 Sortio 도 못 하는 걸 우리가 한다는 증명입니다.

**한계** — 오래된 파일이나 다른 도구를 거친 파일에는 XMP 가 없을 수 있습니다.
그래서 존재 비율(항목 16)을 함께 잽니다. 70% 아래면 이름 기반과 병행해야 합니다.

---

## 13단계 · PSD 헤더

**읽는 것** — 파일 앞 26바이트

```
8BPS  버전  채널수  높이  너비  비트깊이  색공간
```

| 값 | 알아내는 것 | 등급 |
|---|---|---|
| **색공간 (RGB / CMYK)** | **RGB→CMYK 전환은 인쇄 넘기기 직전에만 합니다** ★ | 검증됨 |
| 캔버스 크기 | 같은 작업인지 대조 | 검증됨 |
| PSB 여부 | 대용량 작업 | 검증됨 |

**⚠ 레이어 개수는 신호가 아닙니다.** 직관과 반대로, **최종본일수록 병합하고
평탄화해서 레이어가 줄어듭니다.** `최종_인쇄용.psd` 가 레이어 1개인 경우가
흔합니다. 가장 중요한 파일이 가장 미완성처럼 보입니다.

---

## 14단계 · AI 생성 흔적

**읽는 것** — PNG 조각 구조와 C2PA 흔적

| 값 | 어디서 | 등급 |
|---|---|---|
| PNG `tEXt` `iTXt` `zTXt` 조각 이름 | Stable Diffusion·ComfyUI 가 **프롬프트·시드·모델·워크플로** 를 여기 넣습니다 | 검증됨 |
| C2PA / JUMBF 흔적 | 2026년 현재 Gemini·Firefly·DALL-E·Midjourney 가 기본 탑재 | 검증됨 |

**알아내는 것**

- AI 크리에이터 대응 가치 — 같은 프롬프트 = 같은 시도, 같은 시드 = 변주
- **AI 생성물은 PSD 보다 메타데이터가 오히려 풍부합니다**

**⚠ 성격이 다릅니다.** 이건 "버전"이 아니라 "탐색"입니다. 연사 사진에 가까워요.
그래서 UI 도 시간축이 아니라 **격자**여야 합니다.

**⚠ 한계** — 소셜 플랫폼은 업로드·재인코딩 때 메타데이터를 벗겨냅니다.
카톡·인스타·슬랙을 거친 파일은 이미 비어 있습니다. 낮게 나와도 "원래 없었다"가
아니라 "거쳐 오면서 잃었다" 일 수 있습니다.

**※ 서명을 검증하지 않습니다.** 있는지 없는지만 봅니다. P0 의 목적은 진위
판별이 아니라 '이 증거원을 쓸 수 있는가' 이므로 존재 확인으로 충분합니다.

---

## 15단계 · 썸네일 (qlmanage) — 가장 느림

**읽는 것** — macOS QuickLook 으로 미리보기가 나오는지

```
qlmanage -t -s 128 -o (임시폴더) (파일)
```

**알아내는 것**

**겉모습 비교를 바닥에 깔 수 있는가** ★

macOS 는 앱이 설치돼 있으면 **그 앱의 플러그인으로** 썸네일을 만듭니다.
`.afdesign` `.procreate` `.sketch` `.blend` 가 전부 커버됩니다 — **벤더가 만든
코드에 무임승차**하는 셈입니다.

성공률이 높으면 비어도비 사용자(Figma·Canva·Procreate·AI 생성)도 받을 수
있습니다. 낮으면 어도비 전용 도구가 되고, 나중에 뒤집기 어렵습니다.

**확장자별로 표본만** 검사합니다. 외부 프로세스라 파일마다 돌리면 몇 시간입니다.

---

## 16단계 · 겉모습 지문 (sips)

**읽는 것** — 이미지를 9×9 로 줄여 밝기 격자를 꺼냅니다

```
sips → 9×9 BMP → 밝기값 → dHash 64비트
```

BMP 를 쓰는 이유: 압축이 없어서 표준 라이브러리만으로 픽셀을 읽을 수 있습니다.

**원리** — 가로로 이웃한 두 픽셀 중 어느 쪽이 밝은지만 기록합니다.
밝기 전체가 아니라 **차이의 방향**이라 밝기·대비 변화에 강합니다.

**알아내는 것**

| | 등급 |
|---|---|
| 겉모습만으로 묶이는 가족 수 | **측정값** |

**계보도 이름도 없이 묶을 수 있는가.** Figma·Canva 사용자는 `.fig` 가 아니라
**내보낸 PNG 40개** 가 쌓입니다. 그건 픽셀 비교로만 묶입니다.

**한계** — 겉모습만 봅니다. 초안과 최종이 아예 다르게 생겼으면 못 묶습니다.
그래서 **계보가 있으면 계보가 우선**입니다 (증거 사다리).

**비용 절감** — 같은 폴더에 같은 확장자가 2개 이상인 경우만 후보로 삼습니다.
혼자 있는 이미지는 어차피 가족이 될 수 없으니 검사할 이유가 없습니다.

---

## (선택) · 남의 작업물 — 기본 꺼짐

`--ownership` 으로만 실행됩니다.

**읽는 것** — 파일 앞 512KB + 뒤 256KB 에서 `/Users/누군가/` 를 찾습니다

PSD 의 링크 이미지 경로, INDD 링크, 프로젝트 소스 경로에 다른 사람의 홈 경로가
그대로 저장됩니다. 그 '누구'가 나와 다르면 **다른 사람 컴퓨터에서 만들어진
파일**입니다.

**왜 껐나** — 순서 판단에 안 쓰이는 참고 수치이고, **느려서 표본을 줄인 것이라
얼마나 놓치는지 아직 안 쟀습니다.** 믿을 수 없는 숫자를 기본으로 보여주면
나중에 헷갈립니다. 필요해지면 어디가 느린지 먼저 재고 제대로 고쳐야 합니다.

---

# 증거 사다리 — 한 장 요약

같은 질문에 여러 증거가 답할 수 있습니다. 위쪽을 우선합니다.

## "이 파일들이 한 작업인가?"

| 증거 | 등급 | 단계 |
|---|---|---|
| 같은 `OriginalDocumentID` | **검증됨** | 12 |
| `DerivedFrom` 사슬 | **검증됨** | 12 |
| 겉모습 지문이 가까움 | **측정값** | 16 |
| 같은 캔버스 크기 | 측정값 | 13 |
| 같은 폴더 · 비슷한 시각 | 보조 | 2 |
| 이름 뼈대가 같음 | **추측** | 4 |

## "어느 것이 채택본인가?"

| 증거 | 등급 |
|---|---|
| RGB→CMYK 로 바뀐 버전 | **검증됨** |
| 저장 횟수가 압도적으로 많음 | **검증됨** |
| 다른 파일이 이걸 링크함 | **검증됨** |
| 옆에 내보낸 결과물이 있음 | **보조** ⚠ |
| 수정 시각이 가장 늦음 | **추측** ⚠ |
| 이름에 '최종' 이 있음 | **추측** ⚠ |

**아래 셋에 ⚠ 를 단 이유**

- *옆에 jpg* — 나중 버전에서 이름 안 바꾸고 재내보내기, 확인용 중간 내보내기,
  데스크탑에 내보내기, 슬랙으로 바로 전송. **파일명 관리가 엉망인 사람일수록
  이 증거가 약합니다**
- *수정 시각* — 열어서 저장만 해도 바뀝니다
- *이름* — `최종.psd` 가 `v2.psd` 보다 먼저 만들어진 경우가 흔합니다

## "이 파일이 살아 있나 / 옮겨도 되나"

| 증거 | 등급 |
|---|---|
| 다른 파일이 참조하고 있음 | **검증됨** — 옮기면 깨짐 |
| 클라우드 동기화 폴더 안 | **검증됨** — 관문 1 |
| 앱 라이브러리 번들 안 | **검증됨** — 관문 1 |
| 원본이 옆에 있는 자동 백업 | **검증됨** — 안전하게 삭제 가능 |
| 생성 시각 ≈ 수정 시각 | 보조 — 받아만 놓은 것 |
| 오래 안 건드림 | **추측** |

---

# 제품에서 달라지는 것

**P0 는 표본을 봅니다. 제품은 전부 봐야 합니다.**

| | P0 (조사 도구) | 제품 (2·3편) |
|---|---|---|
| 목적 | 무엇을 먼저 만들지 결정 | 파일을 실제로 분류 |
| 계보 | 큰 것 4,000개 | **전부** |
| 썸네일 | 확장자별 표본 | **전부** |
| 놓치면 | 판단이 조금 흔들림 | **그 파일이 미아가 됨** |

그래서 **한 번 읽고 저장**해야 합니다 (`P1 인벤토리 DB`).

```
저장할 것
  파일 목록 · 크기 · 시각
  XMP 계보 (original_id · 저장 횟수)
  겉모습 지문 (해시 64비트)
  PSD 색공간 등 포맷별 값
  마지막으로 읽은 시각        ← 파일이 안 바뀌었으면 다시 안 읽음

첫 스캔:  몇 분 ~ 몇십 분
그 뒤:    몇 초 (바뀐 것만)
```

**첫 스캔이 오래 걸리는 건 괜찮습니다. 얼어붙은 것처럼 보이는 게 문제입니다.**
예상 시간을 미리 고지하고, 진행 상황이 계속 움직이고, 찾은 것을 보여줘야
합니다. 그 예상 시간은 P0 의 `단계별 소요 시간` 표로 계산합니다.
