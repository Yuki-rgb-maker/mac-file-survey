# 검사기 앱 화면 설계

`p0_survey.py` 를 py2app 으로 `.app` 으로 포장할 때의 화면 명세입니다.

**이 화면은 2편·3편의 첫 스캔 화면 시제품이기도 합니다.**
여기서 만든 것을 제품에서 그대로 씁니다.

---

## 0. 왜 화면이 필요한가

지금은 터미널 출력만 있습니다. 앱으로 만들면 더블클릭했는데 뭐가 보여야 합니다.

그런데 목적이 하나 더 있습니다.

> **실측에서 첫 실행이 3시간 12분 걸렸습니다.**
> 백신이 파일을 처음 열 때마다 전체를 검사하기 때문입니다.
> 두 번째부터는 9분이었습니다.

**오래 걸리는 건 어쩔 수 없습니다. 얼어붙은 것처럼 보이는 게 문제입니다.**

그래서 이 화면의 요구사항은 셋입니다.

| | |
|---|---|
| **예상 시간을 미리 고지** | 시작 전에 "약 N분 걸립니다" |
| **진행 상황이 계속 움직임** | 멈춘 게 아님을 보여줌 |
| **찾은 것을 보여줌** | 기다리는 동안 기대가 쌓임 |

세 번째가 특히 중요합니다. 막대만 움직이면 지루하지만,
"버전 가족 763개 찾음" 이 쌓이면 **결과가 궁금해집니다.**

---

## 1. 전체 흐름

```
① 시작 화면      →  ② 진행 화면      →  ③ 완료 화면
   무엇을 하는지        찾은 것을 쌓음        결과 파일 열기
   안심시키기          남은 시간
```

**뒤로 가기 없음. 설정 없음. 화면 셋뿐입니다.**
조사 도구라 단순해야 합니다.

---

## 2. 시작 화면

```
┌──────────────────────────────────────────────┐
│                                              │
│              맥 파일 상태 검사                  │
│                                              │
│      파일이 어떤 상태인지 숫자로만 셉니다        │
│                                              │
│   ┌────────────────────────────────────┐    │
│   │  · 파일을 하나도 바꾸지 않습니다      │    │
│   │  · 지우거나 옮기지 않습니다          │    │
│   │  · 결과에 파일 이름이 안 들어갑니다   │    │
│   └────────────────────────────────────┘    │
│                                              │
│              [  검사 시작  ]                  │
│                                              │
│         약 1분 걸립니다                       │
│                                              │
└──────────────────────────────────────────────┘
```

### 규칙

- **안심 문구가 버튼보다 위에** 있어야 합니다. 누르기 전에 읽습니다
- 예상 시간은 버튼 **아래**. 눌러도 되는지 판단한 뒤에 보는 정보
- 창 크기 **560×420** 고정 (`DESIGN_SYSTEM` 의 "읽을 게 하나뿐인 화면")

### 예상 시간을 어떻게 내나

`p0_survey.py` 에 이미 있습니다 (`run_with_estimate`).
표본 40개를 **전체에서 고르게** 뽑아 읽어보고, 크기 비율로 환산합니다.

> ⚠ 표본을 앞에서만 뽑으면 안 됩니다. 대상이 크기순으로 정렬돼 있어서
> 앞쪽 40개가 전부 GB 급이 됩니다. 실제로 "39시간 걸립니다" 가 나왔고,
> 실제로는 10분이었습니다.

---

## 3. 진행 화면 ★ 여기가 핵심

```
┌──────────────────────────────────────────────┐
│                                              │
│   파일을 세는 중…                             │
│                                              │
│   ████████████░░░░░░░░░░░░░░░  42%           │
│                                              │
│   1분 12초 경과 · 약 1분 40초 남음             │
│                                              │
│   ┌────────────────────────────────────┐    │
│   │  찾은 것                            │    │
│   │                                     │    │
│   │  파일          58,223 개            │    │
│   │  용량          148.8 GB             │    │
│   │  버전 가족        763 개            │    │
│   │  똑같은 파일     17.6 GB            │    │
│   │  다시 생기는 것   0.7 GB            │    │
│   └────────────────────────────────────┘    │
│                                              │
│                              [ 멈추기 ]      │
└──────────────────────────────────────────────┘
```

### 규칙

- **찾은 것은 위에서부터 한 줄씩 늘어납니다.** 처음엔 비어 있다가 채워집니다
- 이미 나온 숫자는 **사라지지 않습니다.** 계속 쌓입니다
- 숫자가 바뀔 때 **0.2초 정도 부드럽게** (`DESIGN_SYSTEM` 7항)
- `멈추기` 는 **위험색이 아닙니다.** 읽기만 하므로 멈춰도 잃는 게 없습니다

### 단계 이름은 사람 말로

프로그램 내부 이름을 그대로 쓰지 않습니다.

| 내부 | 화면 |
|---|---|
| `파일 훑기` | 파일을 세는 중… |
| `버전 가족 묶기` | 비슷한 파일을 묶는 중… |
| `완전 중복 검사` | 똑같은 파일을 찾는 중… |
| `계보 (XMP)` | 파일 안의 작업 이력을 읽는 중… |
| `썸네일 (qlmanage)` | 미리보기를 확인하는 중… |
| `겉모습 지문 (sips)` | 비슷하게 생긴 것을 찾는 중… |

### 오래 걸릴 때

**`계보` 와 `썸네일` 단계는 몇십 분이 걸릴 수 있습니다.**
실측: 계보 2,268초 · 썸네일 1,129초 (첫 실행 기준)

그 단계에 들어가기 전에 한 번 더 알립니다.

```
┌──────────────────────────────────────────────┐
│   여기부터 오래 걸립니다                       │
│                                              │
│   파일 안을 하나씩 읽습니다. 약 40분 예상.      │
│                                              │
│   백신이 깔려 있으면 더 걸릴 수 있습니다.       │
│   (파일을 처음 열 때마다 검사가 붙습니다)       │
│                                              │
│   중간에 멈추셔도 읽은 것은 저장됩니다.         │
│                                              │
│      [ 계속하기 ]     [ 여기까지만 ]          │
└──────────────────────────────────────────────┘
```

**`여기까지만` 이 정식 선택지입니다.** 1단계만으로도 쓸 만한 결과가 나옵니다.

---

## 4. 완료 화면

```
┌──────────────────────────────────────────────┐
│                                              │
│                    ✓                         │
│                                              │
│              검사가 끝났습니다                  │
│                                              │
│   ┌────────────────────────────────────┐    │
│   │  파일          58,223 개            │    │
│   │  용량          148.8 GB             │    │
│   │  정리할 수 있는 것   22.5 GB         │    │
│   └────────────────────────────────────┘    │
│                                              │
│   결과가 바탕화면에 있습니다                    │
│   맥검사_0810_1530.txt                        │
│                                              │
│   [ 결과 열어보기 ]    [ 폴더에서 보기 ]       │
│                                              │
│   보내시기 전에 열어서 확인해 보셔도 됩니다.     │
│   파일 이름은 들어 있지 않습니다.               │
└──────────────────────────────────────────────┘
```

### 규칙

- **`결과 열어보기` 가 주 버튼입니다.** 확인하고 보내라는 뜻
- 마지막 두 줄이 중요합니다 — **직접 확인할 수 있다**는 게 신뢰의 근거
- 완료 표시는 `DESIGN_SYSTEM` 대로 **원 세 겹 + 흰 체크**
  (진행 표시를 변신시키지 말고 **겹쳐 두고 하나만** 보이게)

---

## 5. 중간에 멈췄을 때

```
┌──────────────────────────────────────────────┐
│   멈췄습니다                                  │
│                                              │
│   지금까지 읽은 것은 저장했습니다.              │
│   다시 실행하면 그 부분은 건너뜁니다.           │
│                                              │
│   여기까지의 결과도 저장했습니다.               │
│   맥검사_0810_1530.txt                        │
│                                              │
│   [ 결과 열어보기 ]        [ 닫기 ]           │
└──────────────────────────────────────────────┘
```

**멈춰도 결과가 남는다는 게 핵심입니다.**
3시간 읽다가 멈췄는데 전부 날아가면 다시 3시간을 내야 합니다.

`p0_survey.py` 에 이미 있습니다 (`ReadCache` · `p0_cache.json`).

---

## 6. 권한 안내

검사 도중 macOS 가 "터미널이 문서 폴더에 접근하려 합니다" 창을 띄웁니다.
앱으로 만들면 앱 이름으로 뜹니다.

**시작 화면에서 미리 알립니다.**

```
   ※ '문서 폴더에 접근하려 합니다' 창이 뜨면 [허용] 을 눌러 주세요.
      안 누르면 그 폴더는 못 셉니다.
```

그리고 **못 읽은 폴더가 있으면 완료 화면에 표시**합니다.

```
   ⚠ 12곳을 못 읽었습니다 (권한 없음)
      시스템 설정 → 개인정보 보호 및 보안 → 전체 디스크 접근
```

> `DECISIONS 7항` — **안 되는 것보다 됐다고 거짓말하는 게 더 나쁩니다.**
> 못 읽었으면 못 읽었다고 해야 합니다.

---

## 7. 만드는 방법

### 무엇으로 그리나

**Tkinter 를 권합니다.**

| | |
|---|---|
| 파이썬 표준 라이브러리 | 추가 설치 없음 |
| py2app 과 궁합 | 검증된 조합 |
| 화면 셋뿐 | 복잡한 UI 프레임워크가 필요 없음 |

*(1편처럼 로컬 서버 + 브라우저 방식도 가능하지만, 조사 도구에는 과합니다)*

### 검사와 화면을 분리합니다

```
p0_survey.py     검사 엔진 — 화면을 모름 (지금 그대로)
p0_app.py        화면 — 엔진을 부르고 진행 상황을 받음
```

**엔진은 건드리지 않습니다.** `StepLog` 와 `Progress` 가 이미 진행 상황을
알려주는 구조라, 콜백만 연결하면 됩니다.

```python
# 지금: print 로 출력
print(f"    {i}/{total} ({pct}%)  {elapsed} 경과 · 약 {left} 남음")

# 앱: 콜백으로 화면에 전달
on_progress(step_name, i, total, elapsed, left, found_so_far)
```

### 화면이 얼어붙지 않게

검사는 **별도 스레드**에서 돌리고, 화면은 주 스레드에서 갱신합니다.
같은 스레드에서 돌리면 검사 중에 창이 아예 안 움직입니다.

### 색

`DESIGN_SYSTEM.md` 의 토큰을 씁니다. 검사기는 시리즈의 일부가 아니므로
강조색은 무채색에 가깝게 두어도 됩니다 — **조사 도구지 제품이 아닙니다.**

---

## 8. 반드시 지킬 것

| | 왜 |
|---|---|
| **`--share` 모드로만 실행** | 결과에 파일 이름이 안 들어가야 합니다 |
| **`--selftest` 가 통과해야 시작** | 옛 파일이면 잘못된 결과가 나옵니다 |
| **파일 이름은 NFC 로** | macOS 는 NFD 로 저장. `PLAN 3-5d` 참조 |
| **읽기 전용** | 삭제·이동 코드가 들어오면 안 됩니다 |
| **못 읽은 것을 숨기지 않기** | `DECISIONS 7항` |

---

## 9. 앱 번들에서 확인해야 할 것

py2app 으로 싸면 실행 환경이 달라집니다. **반드시 시험하세요.**

| | 확인할 것 |
|---|---|
| `qlmanage` · `sips` · `mdfind` | 앱 번들 안에서 `subprocess` 로 부를 수 있나 |
| `~` 경로 | `os.path.expanduser("~")` 가 제대로 도나 |
| 임시 폴더 | `tempfile.gettempdir()` 가 쓰기 가능한가 |
| 바탕화면 쓰기 | 결과 파일을 만들 수 있나 |
| `lang_ko.py` | 번들에 포함됐나 (`import lang_ko` 가 되나) |
| 한글 파일명 | NFD 파일을 제대로 읽나 |

**`--selftest` 를 앱에서도 돌릴 수 있게** 해두면 확인이 쉽습니다.

---

## 10. 배포

```
py2app 으로 .app 만들기
    ↓
zip 으로 압축
    ↓
안내문과 함께 전달
```

**서명·공증은 지금 안 합니다.** 개발자 계정은 3편 프로토타입이 나온 뒤에.
그때까지는 1편에서 쓰던 안내가 그대로 통합니다.

```
⚠ 첫 실행 시 참고해 주세요 (바이러스가 아닙니다!)

애플에 정식 개발자 등록($99)을 하지 않아
"확인되지 않은 개발자" 라고 차단할 수 있습니다.

· 최신 맥: 시스템 설정 → 개인정보 보호 및 보안 → 맨 아래 [그래도 열기]
· 구형 맥: 앱 우클릭 → [열기]

※ 최초 1회만 하면 다음부터는 더블클릭으로 바로 실행됩니다.
```

### 빌드 스크립트는 재사용합니다

`tools/build_app.sh` 로 만들어 두세요.
**3편 배포(P5) 때 서명·공증만 덧붙이면 됩니다.**

```bash
# 지금
py2app → zip

# P5 (개발자 계정 이후)
py2app → codesign → notarytool → stapler → DMG
```
