# 맥 파일 검사 — 앱 배포 안내

`tools/build_app.sh` 로 만든 `맥파일검사.zip` 을 이 안내와 함께 보냅니다.
(서명·공증은 3편 프로토타입 이후에 붙입니다 — 지금은 아래 Gatekeeper 안내로 엽니다.)

---

## 받으신 분께

```
⚠ 첫 실행 시 참고해 주세요 (바이러스가 아닙니다!)

애플에 정식 개발자 등록($99)을 하지 않아
"확인되지 않은 개발자" 라고 차단할 수 있습니다.

· 최신 맥: 시스템 설정 → 개인정보 보호 및 보안 → 맨 아래 [그래도 열기]
· 구형 맥: 앱 우클릭 → [열기]

※ 최초 1회만 하면 다음부터는 더블클릭으로 바로 실행됩니다.
```

이 앱은:

- 파일을 **하나도 바꾸지 않습니다.** 읽기만 하고 숫자를 셉니다.
- 결과 파일에 **파일 이름이 들어가지 않습니다** (`--share` 모드).
- 결과는 **바탕화면**에 `맥검사_MMDD_HHMM.txt` 로 저장됩니다.

## 검사 도중

- "문서/바탕화면 폴더에 접근하려 합니다" 창이 뜨면 **[허용]** 을 눌러 주세요.
  안 누르면 그 폴더는 못 셉니다.
- **처음 실행은 오래 걸릴 수 있습니다.** 백신이 파일을 처음 열 때마다
  검사하기 때문입니다. 얼어붙은 게 아니라 진행 중입니다 — 화면의 진행
  표시와 '찾은 것'이 계속 움직입니다.
- 언제든 **[멈추기]** 를 눌러도 됩니다. 읽기만 하므로 잃는 게 없고,
  여기까지의 결과와 읽어 둔 것은 저장됩니다. 다시 실행하면 이어서 합니다.

## 다 되면

- **[결과 열어보기]** 로 내용을 확인하신 뒤 보내 주세요.
  파일 이름이 없으니 안심하고 열어 보셔도 됩니다.

---

## 혹시 "앱이 손상되었기 때문에 열 수 없습니다" 라고 뜨면

정식 서명 전이라(무료 ad-hoc 서명만 함) 인터넷에서 받은 zip 에
격리 표시가 붙어 이렇게 뜰 수 있습니다. 아래 한 줄이면 풀립니다.

1. **터미널** 을 엽니다 (Spotlight 에서 "터미널" 검색)
2. `xattr -dr com.apple.quarantine ` 까지 입력하고 **한 칸 띄운 뒤**,
   `맥 파일 조사.app` 을 터미널 창으로 **끌어다 놓기** → **Enter**
3. 다시 더블클릭 (또는 우클릭 → 열기)

> 앱은 파일을 읽기만 하고 아무것도 바꾸지 않습니다. 이 명령은 "인터넷에서
> 받음" 표시만 지웁니다.
